"""CS1101 unit 4 — recursion.

    python3 scripts/grade.py CS1101 04
"""


def fact(n):
    """Factorial, recursively: fact(0) == 1, fact(n) == n * fact(n - 1)."""
    raise NotImplementedError


def fib(n):
    """The n-th Fibonacci number, recursively: fib(0) == 0, fib(1) == 1.
    (Plain recursion is fine here — memoization comes later.)"""
    raise NotImplementedError
